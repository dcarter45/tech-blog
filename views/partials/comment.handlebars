<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.username}} 
            {{created_at}}
            
        </div>
        <div class="text">
            {{comment_contents}}
        </div>
    </section>
    {{/each}}
</div>

