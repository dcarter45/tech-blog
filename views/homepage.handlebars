<h1>Tech blog Homepage</h1>



{{#each posts as |post|}}
<div class="postDiv">

    <div class="postID">
        {{post.id}}.
        {{post.author}}.
    </div>

    <div class="postContent">

        {{post.content}}
    </div>

    

</div><br>
{{/each}}